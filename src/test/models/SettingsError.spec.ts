// import { assert } from 'chai';
// import 'mocha';

// import { SettingsError } from '../../models/error/Settings.error';

// describe('SettingsError', () => {
//   it('should have a missing field factory', () => {
//     const actual_value = SettingsError.MISSING_FIELD_TEMPLATE_ERROR('test field');

//     assert.equal(actual_value.message, 'Template section is missing required field \'test field\'');
//   });

//   it('should have a file not found factory', () => {
//     const actual_value = SettingsError.FILE_NOT_FOUND_ERROR('test file');

//     assert.equal(actual_value.message, 'File \'test file\' does not exist!');
//   });
// });