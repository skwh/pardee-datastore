import { assert } from "chai";
import "mocha";

import {
  Response_Category,
  Response,
  make_response
} from "../api";

describe('make_response', () => {
  it('should have tests written for it', () => assert.fail());
});